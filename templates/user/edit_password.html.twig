{%  extends 'base.html.twig' %}

{% block title %}Changer de mot de passe{% endblock %} 


{% block body %}

<div class="container-fluid">


     <div class="row justify-content-center register">

            <div class="col-lg-4 col-md-6 col-sm-9 col-12   mb-5 mt-5">
             <h1 class="text-center  my-5 fs-4">Modification du mot de passe de {{ app.user.firstname}}  </h1>
                {{ form_start(editPassform, {'attr':{'class': 'col '}}) }}
                   {# {{ form_row(editPassform.password) }} #} 
                    {{ form_row(editPassform.plainPassword) }}
                    <div class="text-center"> <a type="submit" class="btn btn-info col-12">Enregister</a></div>  
               {{ form_end(editPassform) }}
            </div>
            
           
    </div>

</div>
{% endblock %}